name: Release CLI

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  release-cli:
    runs-on: macos-latest
    env:
      PYTHON_VERSION: "3.12"
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install build backend
        run: python -m pip install --upgrade build

      - name: Build shared artifacts
        run: |
          cd lib
          python -m build

      - name: Build public CLI artifacts
        run: |
          cd packages/ad-migration-cli
          python -m build

      - name: Smoke-test public CLI in a fresh venv
        run: |
          python -m venv /tmp/ad-migration-release
          source /tmp/ad-migration-release/bin/activate
          python -m pip install lib/dist/ad_migration_shared-*.whl
          python -m pip install packages/ad-migration-cli/dist/ad_migration_cli-*.whl
          ad-migration --version
          deactivate
          /tmp/ad-migration-release/bin/ad-migration --version

      - name: Resolve release version from pyproject metadata
        id: version
        run: |
          set -euo pipefail
          project_version="$(python - <<'PY'
          import tomllib
          from pathlib import Path

          shared = tomllib.loads(Path("lib/pyproject.toml").read_text())["project"]["version"]
          cli = tomllib.loads(Path("packages/ad-migration-cli/pyproject.toml").read_text())["project"]["version"]
          if shared != cli:
              raise SystemExit(f"Version mismatch: lib={shared} packages/ad-migration-cli={cli}")
          print(shared)
          PY
          )"
          tag_version="${GITHUB_REF_NAME#v}"
          if [ "$project_version" != "$tag_version" ]; then
            echo "::error::Tag version ${tag_version} does not match pyproject version ${project_version}"
            exit 1
          fi
          echo "project_version=${project_version}" >> "$GITHUB_OUTPUT"

      - name: Delete stale package assets before upload
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail

          existing_assets="$(gh release view "$GITHUB_REF_NAME" --json assets --jq '.assets[].name' 2>/dev/null || true)"
          if [ -z "$existing_assets" ]; then
            echo "No existing release assets to clean for ${GITHUB_REF_NAME}."
            exit 0
          fi

          while IFS= read -r asset; do
            case "$asset" in
              ad_migration_shared-*.whl|ad_migration_shared-*.tar.gz|ad_migration_cli-*.whl|ad_migration_cli-*.tar.gz)
                echo "Deleting stale package asset: ${asset}"
                gh release delete-asset "$GITHUB_REF_NAME" "$asset" --yes
                ;;
            esac
          done <<< "$existing_assets"

      - name: Publish GitHub release artifacts
        uses: softprops/action-gh-release@v3
        with:
          files: |
            lib/dist/*
            packages/ad-migration-cli/dist/*

      - name: Compute Homebrew artifact metadata
        id: artifacts
        run: |
          set -euo pipefail
          version="${{ steps.version.outputs.project_version }}"
          cli_sdist="$(basename packages/ad-migration-cli/dist/ad_migration_cli-"${version}".tar.gz)"
          shared_sdist="$(basename lib/dist/ad_migration_shared-"${version}".tar.gz)"
          cli_sha="$(shasum -a 256 "packages/ad-migration-cli/dist/${cli_sdist}" | awk '{print $1}')"
          shared_sha="$(shasum -a 256 "lib/dist/${shared_sdist}" | awk '{print $1}')"
          release_base="https://github.com/${GITHUB_REPOSITORY}/releases/download/${GITHUB_REF_NAME}"
          echo "version=${version}" >> "$GITHUB_OUTPUT"
          echo "cli_url=${release_base}/${cli_sdist}" >> "$GITHUB_OUTPUT"
          echo "cli_sha256=${cli_sha}" >> "$GITHUB_OUTPUT"
          echo "shared_url=${release_base}/${shared_sdist}" >> "$GITHUB_OUTPUT"
          echo "shared_sha256=${shared_sha}" >> "$GITHUB_OUTPUT"

      - name: Update Homebrew tap formula
        env:
          HOMEBREW_TAP_PAT: ${{ secrets.HOMEBREW_TAP_PAT }}
        run: |
          set -euo pipefail
          if [ -z "${HOMEBREW_TAP_PAT:-}" ]; then
            echo "::error::HOMEBREW_TAP_PAT secret is required to update accelerate-data/homebrew-tap"
            exit 1
          fi

          tap_dir="$(mktemp -d)"
          git clone "https://x-access-token:${HOMEBREW_TAP_PAT}@github.com/accelerate-data/homebrew-tap.git" "$tap_dir"
          python scripts/update_homebrew_tap.py \
            --version "${{ steps.artifacts.outputs.version }}" \
            --cli-url "${{ steps.artifacts.outputs.cli_url }}" \
            --cli-sha256 "${{ steps.artifacts.outputs.cli_sha256 }}" \
            --shared-url "${{ steps.artifacts.outputs.shared_url }}" \
            --shared-sha256 "${{ steps.artifacts.outputs.shared_sha256 }}" \
            --output "$tap_dir/Formula/ad-migration.rb"

          cd "$tap_dir"
          if git diff --quiet -- Formula/ad-migration.rb; then
            echo "Homebrew formula already up to date."
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add Formula/ad-migration.rb
          git commit -m "Update ad-migration formula for ${GITHUB_REF_NAME}"
          git push origin HEAD
