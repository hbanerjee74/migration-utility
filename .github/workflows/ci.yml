name: CI

on:
  push:
    branches: [main]
    paths-ignore:
      - 'LICENSE'
  pull_request:
    branches: [main]
    paths-ignore:
      - 'LICENSE'

permissions:
  contents: read

jobs:
  python-dead-code-lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Set up uv
        uses: astral-sh/setup-uv@v7

      - name: Check Python unused imports and locals
        run: |
          uvx ruff check \
            lib/shared \
            packages/ad-migration-cli/src \
            packages/ad-migration-internal/src \
            mcp/ddl \
            scripts \
            tests/helpers.py \
            tests/conftest.py \
            tests \
            mcp/ddl/tests \
            --select F401,F841 \
            --statistics

  unit-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Set up uv
        uses: astral-sh/setup-uv@v7

      - name: Install database driver system packages
        run: sudo apt-get update && sudo apt-get install -y freetds-bin freetds-dev tdsodbc unixodbc-dev

      - name: Shared library unit tests with coverage
        working-directory: lib
        run: uv run --with pytest-cov --with pyodbc --with oracledb pytest --cov=shared --cov-report=term-missing --cov-fail-under=70

      - name: DDL MCP unit tests
        working-directory: mcp/ddl
        run: uv run pytest

  repo-map-audit:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Check for structural changes requiring repo-map.json update
        run: |
          BASE_SHA="${{ github.event.pull_request.base.sha }}"
          HEAD_SHA="${{ github.event.pull_request.head.sha }}"

          STRUCTURAL_DIRS=(
            ".claude-plugin/"
            ".codex-plugin/"
            ".github/workflows/"
            "skills/"
            "commands/"
            "lib/shared/"
            "packages/"
            "mcp/ddl/"
            "tests/unit/"
          )

          structural_changed=false
          for dir in "${STRUCTURAL_DIRS[@]}"; do
            if git diff --name-only --diff-filter=ADR "$BASE_SHA" "$HEAD_SHA" -- "$dir" | grep -q .; then
              structural_changed=true
              echo "Structural change detected in $dir"
            fi
          done

          if [ "$structural_changed" = true ]; then
            if git diff --name-only "$BASE_SHA" "$HEAD_SHA" -- repo-map.json | grep -q .; then
              echo "repo-map.json was updated — audit passed."
            else
              echo "::error::Structural files were added, removed, or renamed but repo-map.json was not updated."
              echo "Update repo-map.json to reflect the current codebase. See AGENTS.md for details."
              exit 1
            fi
          else
            echo "No structural changes detected — audit passed."
          fi

  design-index-audit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Check design docs index
        run: python scripts/check_design_index.py

  version-consistency-audit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Check package and plugin versions
        run: python scripts/check_version_consistency.py

  plugin-manifest-audit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Validate Claude and Codex plugin manifests
        run: python scripts/validate_plugin_manifests.py

  public-surface-docs-audit:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Check public surface docs
        run: |
          python scripts/check_public_surface_docs.py \
            --base "${{ github.event.pull_request.base.sha }}" \
            --head "${{ github.event.pull_request.head.sha }}"
